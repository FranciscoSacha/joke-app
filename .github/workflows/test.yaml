name: Test workflow

on: 
  push:
    branches:
      - "main"
    
jobs:
  echo:
    runs-on: ubunutu-latest
    steps:
      - name: Echo hello world
        run: |
          export NAME=Sacha
          echo $NAME